# System Manager Environment Configuration
# Copy to .env and modify as needed

# Set to 1 to disable access code authentication (development only)
# Set to 0 or remove for production with authentication
DISABLE_AUTH=0

# Host root prefix for containerized file reads (set automatically in docker-compose.yml)
# HOST_ROOT=/host